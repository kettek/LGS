# -----------------------------------------------
# !!! THIS FILE WAS GENERATED BY TYPE-GRAPHQL !!!
# !!!   DO NOT MODIFY THIS FILE BY YOURSELF   !!!
# -----------------------------------------------

interface Action implements UniqueID {
  actionType: ActionType!
  exercise: String!
  id: ID!
}

type ActionAerobic implements Action & UniqueID {
  actionType: ActionType!
  distance: Float
  duration: Float
  exercise: String!
  id: ID!
  repetitions: Int
  weight: Float
}

type ActionAnaerobic implements Action & UniqueID {
  actionType: ActionType!
  exercise: String!
  id: ID!
  repetitions: Int
  weight: Float
}

type ActionEat implements Action & UniqueID {
  actionType: ActionType!
  duration: Float
  exercise: String!
  food: String!
  id: ID!
}

type ActionFlexibility implements Action & UniqueID {
  actionType: ActionType!
  exercise: String!
  id: ID!
  repetitions: Int
  weight: Float
}

type ActionRest implements Action & UniqueID {
  actionType: ActionType!
  duration: Float
  exercise: String!
  id: ID!
}

"""The type of action performed."""
enum ActionType {
  AEROBIC
  ANAEROBIC
  EAT
  FLEXIBILITY
  REST
}

union Actions = ActionAerobic | ActionAnaerobic | ActionEat | ActionFlexibility | ActionRest

type Query {
  actions: [Actions!]!
}

interface UniqueID {
  id: ID!
}
